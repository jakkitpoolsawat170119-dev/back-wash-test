/* โหนด "Duty Gate" ของ workflow "Telegram Production Chart Bot" — ฉบับกันพัง
   (n8n → เปิดโหนด Duty Gate → แก้ Conditions ตามขั้นตอนข้างล่าง)

   🔑 topology จริง (ยืนยันจากแคนวาส execution 2026-09-02) — ต่อกันเป็นเส้นเดียว
      ทุกสายห้อยอยู่กับ False Branch ของประตู ประตูผิดด้านเมื่อไหร่ = พังทันที:

        Telegram Trigger → Duty Gate ─[T]→ Forward to Duty → /api/telegram/duty-update
                                     └[F]→ Check CIP Summary → If → Send CIP Chart
                                        └→ Is Assistant Msg ─[T]→ Ask Assistant → Send Assistant Reply
                                                            └[F]→ Parse Query → Build Chart → Send Chart

      True ตลอด  = กราฟ · สรุป CIP · ผู้ช่วย AI เงียบหมด
      False ตลอด = กระดานงาน (เช็คงาน/งานค้าง/ปิดงาน) + ปุ่มทุกปุ่มบนกระดานเงียบหมด

   ── ประวัติอาการ 2026-09-02 ─────────────────────────────────────────────
   รอบที่ 1 · True ตลอด — วางโค้ดสไตล์ Code node (หลายบรรทัด มี const) ลงช่องซ้าย
     โดยไม่มี {{ }} ครอบ · n8n ประเมินเฉพาะสิ่งที่อยู่ใน {{ }} ไม่มีก็คืนตัวโค้ดทั้งก้อน
     เป็นสตริง ซึ่งข้างในมีคำว่า __DUTY__ อยู่ → regex เจอทุกครั้ง
   รอบที่ 2 · False ตลอด — ใส่ {{ }} + operator "is equal to" + ช่องขวา DUTY ครบแล้ว
     แต่ค่าที่ออกมาไม่เท่ากับ DUTY เป๊ะ (ตัวต้องสงสัย: ขึ้นบรรทัดใหม่/ช่องว่างท้าย }}
     ทำให้ได้ "DUTY\n" — กับดักตัวเดียวกับที่เคยทำให้ IF boolean คืน "true\n")
     → เวอร์ชันนี้เลยเปลี่ยนไปใช้ "contains" ซึ่งไม่แคร์ช่องว่างท้ายบรรทัด

   🔑 บทเรียน: ช่อง IF รับได้แค่ expression บรรทัดเดียวใน {{ }}
      และห้ามใช้ตัวเปรียบเทียบที่ซีเรียสกับช่องว่าง (is equal to / boolean)

   ── วิธีตั้งค่า (ต้องครบทั้ง 3 ช่อง) ────────────────────────────────────
   1. ช่องซ้าย  : ลบของเดิมทิ้ง วางบรรทัด {{ ... }} ข้างล่างนี้
   2. Operator  : String → contains
   3. ช่องขวา   : GO-DUTY
   4. Convert types where required: ปิดไว้
   5. เช็คก่อน Save: กดที่ช่องซ้าย ดูบรรทัด Result
        "เช็คงาน"  ต้องได้ GO-DUTY      "สรุป CIP"  ต้องได้ NO
   6. ทดสอบในกลุ่มจริงทั้ง 2 ฝั่งเสมอ — "เช็คงาน" (ต้องได้กระดาน) และ
      "สรุป CIP" (ต้องได้กราฟ) · ทดสอบฝั่งเดียวจับอาการกลับด้านไม่ได้

   ── ทำไมถึงกันพังได้ 2 ชั้น ─────────────────────────────────────────────
   ชั้นที่ 1: "contains" ทนขึ้นบรรทัดใหม่/ช่องว่างท้าย }} (ทดสอบแล้ว "GO-DUTY\n" ผ่าน)
   ชั้นที่ 2: marker เขียนแยกเป็น 'GO' + '-DUTY' โดยตั้งใจ — ถ้าวันหน้าเผลอทำ {{ }}
             หลุดอีก n8n จะเอาตัวโค้ดดิบมาเทียบ ซึ่ง "ไม่มีคำว่า GO-DUTY ติดกัน"
             → ตกไป False Branch = สายใบตรวจพังแบบเห็นชัดทันที (พิมพ์ "เช็คงาน"
             แล้วไม่มีกระดาน) แทนที่จะปล่อยผ่านทุกอย่างเงียบ ๆ แบบรอบที่ 1
             ⚠️ ห้ามรวมเป็น 'GO-DUTY' คำเดียว จะเสียคุณสมบัติข้อนี้ทันที

   ── ผ่าน / ตัดทิ้ง ──────────────────────────────────────────────────────
   ผ่าน   : p: (บอร์ดรายคน/ใบตรวจ) · t: (ติ๊กงาน) · sum (ส่งสรุป) · คำสั่งเรียกกระดาน · รูปภาพ
   ตัดทิ้ง: inc: (แจ้งเหตุการณ์) · m: (กระดานซ่อมบำรุง) · mpm: (ตั้งงาน PM) — ย้ายไปบอทช่างแล้ว

   ⚠️ ปุ่มที่เพิ่มใหม่ในกระดานบอทผลิตต้องใช้ prefix ที่ประตูปล่อยผ่าน (p: t: sum)
      ไม่งั้นกดแล้วเงียบ — ดูกติกาเหนือ buildDutyHome() ใน server/index.js
*/

// ───── วางบรรทัดนี้ในช่องซ้ายของ Conditions (ทั้งบรรทัด รวม {{ }}) ─────

{{ /^[pt]:/.test((($json.callback_query || {}).data) || '') || ((($json.callback_query || {}).data) || '') === 'sum' || !!(($json.message || {}).photo) || /^\/(start|menu|duty)\b|^เมนู$|ปิดงาน|งานค้าง|เช็[กค]\s*งาน|หน้าที่/.test(((($json.message || {}).text) || '').trim()) ? 'GO' + '-DUTY' : 'NO' }}

/* หมายเหตุ: จงใจไม่ใช้ optional chaining (?.) — เขียนเป็น ($json.x || {}).y แทน
   เพื่อให้ทำงานได้กับ expression engine ทุกเวอร์ชันของ n8n */
