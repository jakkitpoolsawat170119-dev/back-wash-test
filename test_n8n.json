{{
 {
  "messages": [
   {
    "type": "text",
    "text": "[รายงาน CIP ไลน์ 2]\n\n" +
      "✅ ขั้นตอน: " + $json.body.stepDescription + "\n" +
      "👤 ผู้ทำ: " + $json.body.operatorName + "\n" +
      "⏰ เวลา: " + ($json.body.startTime ? new Date($json.body.startTime).toLocaleTimeString('th-TH', {hour: '2-digit', minute:'2-digit'}) : '-') + " - " + new Date($json.body.endTime).toLocaleTimeString('th-TH', {hour: '2-digit', minute:'2-digit'}) + "\n" +
      "📊 แรงดัน: " + $json.body.pressure + " bar\n" +
      "🍭 Brix: " + $json.body.brix + " %\n" +
      "🧪 pH: " + $json.body.ph + "\n" +
      "📝 หมายเหตุ: " + ($json.body.remarks || '-') + "\n" +
      "📷 รูปถ่าย: " + ($json.body.image || 'ไม่มีรูปถ่าย')
   }
  ]
 }
}}
